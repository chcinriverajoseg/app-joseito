# app-joseito — Frontend baseline estable (recuperación)
1) Copia este folder dentro de tu carpeta `frontend` del proyecto (o descomprime el ZIP).
2) `npm i`
3) `npm run dev`
Base API: `http://localhost:4000/api` (ajusta en `src/api/axios.js`).
Rutas con `:chatId` y página `Perfil` incluidas.
